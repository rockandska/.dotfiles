[${HOME}/.local/opt/base16-shell]
lib = 
	GIT_REF=efa06ce
checkout =
	git clone https://github.com/chriskempson/base16-shell.git ${MR_REPO} || rm -rf ${MR_REPO}
	cd ${MR_REPO} && git checkout ${GIT_REF}
