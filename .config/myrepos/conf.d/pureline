[${HOME}/.local/opt/pureline]
lib = 
	GIT_REF=da1377fb6872b480cc40dfbaa6ad2e9741259266
checkout =
	git clone https://github.com/rockandska/pureline.git ${MR_REPO} || rm -rf ${MR_REPO}
	cd ${MR_REPO} && git checkout ${GIT_REF}
