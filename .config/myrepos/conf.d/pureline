[${HOME}/.local/opt/pureline]
lib = 
	GIT_REF=318458905453de42b6c57df0a49c7871ee150afe
checkout =
	git clone https://github.com/chris-marsh/pureline.git ${MR_REPO} || rm -rf ${MR_REPO}
	cd ${MR_REPO} && git checkout ${GIT_REF}
