[${HOME}/.local/opt/oh-my-git]
lib = 
	GIT_REF=WIP_new_refactoring
checkout =
	git clone https://github.com/rockandska/oh-my-git.git ${MR_REPO} || rm -rf ${MR_REPO}
	cd ${MR_REPO} && git checkout ${GIT_REF}
